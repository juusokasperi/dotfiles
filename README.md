## Setup


```bash
# Clone the repo
git clone https://github.com/juusokasperi/dotfiles.git
# Add the tmux configuration symbolic link
cd dotfiles && ./bootstrap.sh
# Optional disk utility script for weekly runs:
cd dotfiles && ./du_bootstrap.sh
```
